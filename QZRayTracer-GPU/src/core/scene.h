#ifndef QZRT_CORE_SCENE_H
#define QZRT_CORE_SCENE_H
#include "QZRayTracer.h"
namespace raytracer {
	/*__global__ inline void FreeWorld(RendererSet& set) {
		for (int i = 0; i < ((ShapeList*)set.shapes)->numShapes; i++) {
			delete ((Sphere*)set.shapes[i])->material;
			delete set.shapes[i];
		}
		delete* set.shapes;
		delete* set.camera;
	}*/
}
#endif

