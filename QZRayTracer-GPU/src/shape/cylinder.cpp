#include "cylinder.h"

namespace raytracer {
	
	/*Shape* CreateCylinderShape(Point3f center, Float radius, Float zMin, Float zMax, Material* material) {
		return new Cylinder(center, radius, zMin, zMax, material);
	}*/
}
